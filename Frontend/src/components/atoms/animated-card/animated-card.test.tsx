import React from 'react'
import { render, screen } from '@testing-library/react'
import { AnimatedCar } from './animated-card'
import { AnimatedCarProps } from './animated-card.props'

describe('AnimatedCar', () => {
  const {} = props

  it('should render', () => {
    // Given

    // When

    // Then
    expect()
  })
})
